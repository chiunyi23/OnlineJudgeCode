#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    char data;
    struct Node* left;
    struct Node* right;
} Node;

int main()
{
    int num
    while(scanf("%d", &num) != EOF) {
        Node* root = NULL;
        char element[26];
        int child[26][2];
        
        for(int i = 0; i < num; i++) 
            scanf("%c %d %d", &element[i], &child[i][0], &child[i][1]);
        } 
        for(int i = 0; i < num; i++) {
            
        }

    return 0;
}